{
  "name": "zyre",
  "version": "0.0.5",
  "description": "Zyre",
  "scripts": {
    "install": "prebuild --install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "rebuild": "prebuild --compile",
    "prebuild": "prebuild --strip --verbose"
  },
  "author": "ZeroMQ Community",
  "license": "MPL-2.0",
  "gypfile": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/hintjens/zyre-nodejs"
  },
  "dependencies": {
    "bindings": "^1.2.1",
    "prebuild": "^3.0.3"
  },
  "devDependencies": {
    "nan": "^2.2.0"
  }
}
